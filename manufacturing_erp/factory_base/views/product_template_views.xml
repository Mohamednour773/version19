<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_product_template_form_factory" model="ir.ui.view">
        <field name="name">product.template.form.factory</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='general_information']" position="after">
                <page string="Factory Settings | إعدادات المصنع" name="factory_settings">
                    <group>
                        <group>
                            <field name="is_factory_product"/>
                            <field name="factory_product_type" invisible="not is_factory_product"/>
                        </group>
                        <group>
                            <field name="standard_waste_percent" invisible="not is_factory_product"/>
                        </group>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

</odoo>
