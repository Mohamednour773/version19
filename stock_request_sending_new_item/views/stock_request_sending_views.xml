<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_stock_request_sending_form" model="ir.ui.view">
        <field name="name">stock.request.sending.form</field>
        <field name="model">stock.request.sending</field>
        <field name="arch" type="xml">
            <form string="Stock Request Sending">
                <header>
                    <button name="action_confirm" string="Confirm" type="object" class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_cancel" string="Cancel" type="object" invisible="state not in ('draft', 'confirmed')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,confirmed,done"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_pickings" type="object" class="oe_stat_button" icon="fa-truck">
                            <field name="picking_count" widget="statinfo" string="Pickings"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="source_warehouse_id" options="{'no_create': True}" readonly="state != 'draft'"/>
                            <field name="destination_warehouse_id" options="{'no_create': True}" readonly="state != 'draft'"/>
                        </group>
                        <group>
                            <field name="company_id" groups="base.group_multi_company" readonly="state != 'draft'"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Products" name="products">
                            <field name="line_ids" readonly="state != 'draft'">
                                <list editable="bottom">
                                    <field name="product_id"/>
                                    <field name="quantity"/>
                                    <field name="uom_id" groups="uom.group_uom"/>
                                    <field name="note"/>
                                </list>
                                <form>
                                    <group>
                                        <group>
                                            <field name="product_id"/>
                                            <field name="quantity"/>
                                            <field name="uom_id" groups="uom.group_uom"/>
                                        </group>
                                        <group>
                                            <field name="note"/>
                                        </group>
                                    </group>
                                </form>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="view_stock_request_sending_tree" model="ir.ui.view">
        <field name="name">stock.request.sending.tree</field>
        <field name="model">stock.request.sending</field>
        <field name="arch" type="xml">
            <list string="Stock Requests">
                <field name="name"/>
                <field name="source_warehouse_id"/>
                <field name="destination_warehouse_id"/>
                <field name="state" widget="badge" decoration-info="state == 'draft'" decoration-success="state == 'confirmed'"/>
            </list>
        </field>
    </record>

    <record id="action_stock_request_sending" model="ir.actions.act_window">
        <field name="name">Stock Requests</field>
        <field name="res_model">stock.request.sending</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new stock transfer request.
            </p>
        </field>
    </record>

    <menuitem id="menu_stock_request_sending_root" 
              name="Stock Requests" 
              parent="stock.menu_stock_root" 
              sequence="25"/>

    <menuitem id="menu_stock_request_sending" 
              name="Inter-Branch Requests" 
              parent="menu_stock_request_sending_root" 
              action="action_stock_request_sending" 
              sequence="10"/>
</odoo>
